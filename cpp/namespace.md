# Namespace

Used to organize code and avoid naming conflicts.
You can create new namespaces with `namespace name{class Example{}}`.
Then you can access it with `name::Example` or you can just use `using namespace name`
to avoid re-writting it in the current scope.
